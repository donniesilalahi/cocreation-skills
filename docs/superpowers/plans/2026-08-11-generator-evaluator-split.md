# Generator/Evaluator Split Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rename `coframe`→`codirect` and `coverify`→`cotest`, add the new `cochallenge` devil's-advocate loop, and rewire workflows/manifests/docs — per spec `docs/superpowers/specs/2026-08-11-generator-evaluator-split-design.md`.

**Architecture:** This is a docs/markdown skill ecosystem (no app code, no unit tests). "Tests" = `npm run validate` (SKILL.md rules), `npm run update-indices`, `npm run build-manifests`, and grep sweeps proving zero stale references. Manifests and `agents/` + `.opencode/agents/` files are GENERATED by `scripts/build-plugin-manifests.js` from its `ROSTER`/`PURPOSES`/`EXTRA_STEPS` constants — never hand-edit generated files.

**Tech Stack:** Plain Node.js ES modules (`node:*` only), zero deps, Node >= 18.

## Global Constraints

- SKILL.md frontmatter: ONLY `name`, `description`, `license`, `compatibility`, `metadata`, `allowed-tools`. `name` == dirname, kebab-case, ≤64 chars. `description` ≤1024 chars. Whole file ≤500 lines.
- Memory-bank index = first ALL-CAPS `.md` file in `memory-bank/` (regex `^[A-Z]+\.md$`); frontmatter `columns:` spec; regenerated by the skill's `index.mjs` — never hand-edit the table.
- Status lives in frontmatter `status:` field, never folder/filename.
- Generated files (do NOT hand-edit): `agents/*.md`, `.opencode/agents/*.md`, `.claude-plugin/*.json`, `.codex-plugin/*.json`, `.cursor-plugin/*.json`, `.agents/plugins/marketplace.json`.
- Leave HISTORICAL records untouched: `CHANGELOG.md` past entries, `docs/cocreator/specs/2026-08-05-costudy.md` (dated spec, historical), git history.
- Rename mapping (apply everywhere else): `coframe`→`codirect` · `coframer`→`codirector` · `FRAME.md`→`DIRECTION.md` · `coverify`→`cotest` · `coverifier`→`cotester` · `QA.md`→`TEST.md`. Adjust surrounding grammar ("the frame loop"→"the direct loop" etc.) — this is prose, not sed.
- Version: bump `package.json` to **0.15.0** (0.14.0 already published).
- Conventional Commits; work stays on branch `feat/generator-evaluator-split`; PR to `main` at the end.

---

### Task 1: Rename `coframe` → `codirect`

**Files:**
- Rename: `skills/coframe/` → `skills/codirect/` (git mv)
- Rewrite: `skills/codirect/SKILL.md`
- Rename: `skills/codirect/memory-bank/FRAME.md` → `DIRECTION.md` (git mv) + edit
- Modify: `skills/codirect/memory-bank/_template.md`
- Keep: `skills/codirect/index.mjs` unchanged (generic — finds the ALL-CAPS index by pattern)

**Interfaces:**
- Produces: skill name `codirect`, doer name `codirector`, index `DIRECTION.md` — used by Tasks 4–7.

- [ ] **Step 1: git mv the directory and index**

```bash
git mv skills/coframe skills/codirect
git mv skills/codirect/memory-bank/FRAME.md skills/codirect/memory-bank/DIRECTION.md
```

- [ ] **Step 2: Rewrite `skills/codirect/SKILL.md`** with exactly:

```markdown
---
name: codirect
description: The "direct" loop — the Director of Product seat. Set the product direction and the design direction at direction level, fix the appetite (the time/effort box), and write a tight pitch an agent can't wander from — pulling risk forward by naming the load-bearing assumptions before any building starts. Product direction = problem framing, appetite, pitch (Shape Up "shaping"). Design direction = design principles, mood, references, brand positioning — the direction of the design, not its execution (tokens/screens/states stay in cospecify/codraw). The doer is codirector. Use upstream, at the strategy stage, for high-blast-radius or ambiguous work where getting the direction wrong is expensive. Maps to Double Diamond "Define" and Shape Up shaping. Optional loop; runs standalone or hands a directed pitch to cochallenge (stress-test), coresearch (evidence), or coplan (decompose). Best on Opus (judgment-heavy). Formerly named coframe.
---

# codirect — direct the product

The doer is **codirector**. This is the optional **direct** loop at the upstream strategy
stage — judgment-heavy, best run on **Opus**. Effort concentrates here: a tight direction and a
fixed appetite are worth more than a fast build downstream. It decides the **direction** (the
problem, and how big a box to spend on it) — distinct from `coplan` (which decides the *work*)
and `cospecify` (which decides the *solution*).

## Scope — direction level only

Two dimensions, one altitude:

- **Product direction** — the real need, who it's for, what's out of scope, the appetite, the
  pitch. Inherited unchanged from Shape Up "shaping".
- **Design direction** — design principles, mood, references, brand positioning. The *direction*
  of the design, never its execution: tokens, screens, states, and data shapes belong to
  `cospecify` / `codraw` / the design-manifest.

## When to Use

- Work is ambiguous, high-blast-radius, or "what should we even build?" is unsettled.
- You need a box around effort before an agent starts — fixed appetite, variable scope.
- The riskiest part is the direction, not the execution.
- The product needs a design direction named before specs and artboards exist.

## How to Use

1. **Recall first** — run `colearn` to pull lessons relevant to this problem space.
2. **Direct the product** — what's the real need, who's it for, what's explicitly out of scope.
3. **Set the appetite** — a fixed time/effort box (e.g. "small batch: 2 days"). Scope flexes
   to fit; the box does not grow.
4. **Name the design direction** (when the work has a visual surface) — principles, mood,
   references, brand position. One paragraph, not a token sheet.
5. **Write the pitch** — tight enough that an agent can't wander: problem, appetite, the shape of
   a solution, rabbit holes to avoid, no-gos. Use `EnterPlanMode` to draft it.
6. **Pull risk forward** — name the load-bearing assumptions and what would make each fail
   (a pre-mortem). Flag the cheapest to test for `coresearch`.

## Native tooling

`EnterPlanMode` (draft and pressure-test the pitch) · **Agent** (delegate exploratory probes) ·
hand the directed pitch to `cochallenge` (stress-test it), `coresearch` (test the risk), or
`coplan` (decompose), and any direction lesson to `colearn`.

## Memory Bank

Save each pitch as `YYYY-MM-DD-kebab-title.md`:
```
.agents/skills/codirect/memory-bank/2026-06-26-onboarding-revamp.md
```
Copy `_template.md`, keep its frontmatter (`title`, `date`, `appetite`, `status`) filled.
`DIRECTION.md` is the auto-generated index — never edit it by hand. After changes:
`node .agents/skills/codirect/index.mjs`.

## Self-eval gate (close the loop)

- **Pitch is tight, appetite set, risks named** → PASS to `cochallenge` (stress-test the
  direction) or `coresearch` (test the risk) or `coplan`.
- **Direction is fuzzy or pitch is wanderable** → re-loop: re-direct.
- **Needs a product/business decision** → escalate to the human.
- **Too big for the appetite** → cut scope to fit the box (fixed appetite); don't grow it.
- **`cochallenge` returns COLLAPSES or HOLED** → this loop re-runs with the hole list; the
  challenger never edits the pitch itself.

## Principles

- Fixed appetite, variable scope: cut to fit the box, default to cancel not extend.
- A pitch an agent can wander from isn't directed yet — name the rabbit holes and no-gos.
- The cheapest bug is the one you named before building — list load-bearing assumptions.
- Direction is judgment, not execution; spend human attention here.
- Direction level only: the moment you're writing tokens or screen states, you're in
  `cospecify`'s seat — stop and hand off.
- Close the loop: feed direction lessons back to `colearn`.
```

- [ ] **Step 3: Edit `skills/codirect/memory-bank/DIRECTION.md`** — change its heading/intro prose from "frame"/"FRAME" wording to "direction"/"DIRECTION" (keep the `columns:` frontmatter line exactly as it was in FRAME.md, and keep the auto-generated marker + table untouched).

- [ ] **Step 4: Edit `skills/codirect/memory-bank/_template.md`** — apply the rename mapping to any prose ("frame the problem" → "direct the product", `coframe` → `codirect`). Keep frontmatter fields (`title`, `date`, `appetite`, `status`) unchanged.

- [ ] **Step 5: Verify**

Run: `node skills/codirect/index.mjs && node scripts/validate-skills.js`
Expected: `updated codirect/DIRECTION.md (0 entries)`; validate passes for codirect (it may fail on other skills only if unrelated — it should not).

- [ ] **Step 6: Commit**

```bash
git add -A skills/codirect
git commit -m "feat: rename coframe to codirect (Director of Product seat)"
```

---

### Task 2: Rename `coverify` → `cotest`

**Files:**
- Rename: `skills/coverify/` → `skills/cotest/` (git mv)
- Modify: `skills/cotest/SKILL.md` (rename sweep — behavior unchanged)
- Rename: `skills/cotest/memory-bank/QA.md` → `TEST.md` (git mv) + edit
- Modify: `skills/cotest/memory-bank/_template.md`

**Interfaces:**
- Produces: skill name `cotest`, doer name `cotester`, index `TEST.md` — used by Tasks 4–7.

- [ ] **Step 1: git mv**

```bash
git mv skills/coverify skills/cotest
git mv skills/cotest/memory-bank/QA.md skills/cotest/memory-bank/TEST.md
```

- [ ] **Step 2: Edit `skills/cotest/SKILL.md`** — this is a rename, NOT a rewrite. Apply exactly these changes:
  - Frontmatter `name: cotest`.
  - Frontmatter `description:` replace with: `The "test" loop — QA testing against the spec. Systematic QA of built output against Open Design artboards or design specs, and nothing beyond the spec: cross-checking is the whole scope (deeper adversarial evaluation is cochallenge; product-vs-job evaluation is cocritique). Compares marketing captures or live UI against reference designs, documents discrepancies, and produces actionable fix lists with pixel-perfect guidance. The doer is cotester. Use when validating screens after implementation or during design review. Runs on Haiku — checklist cross-checking is mechanical; the verification-trap guardrails still apply. Formerly named coverify.`
  - Heading `# Design QA Skill` → `# cotest — test against the spec`.
  - `The doer is **coverifier**` → `The doer is **cotester**`.
  - Every `coverify` → `cotest`, every `QA.md` → `TEST.md`, memory-bank paths `.agents/skills/coverify/` → `.agents/skills/cotest/`, `node .agents/skills/coverify/index.mjs` → `node .agents/skills/cotest/index.mjs`.
  - In "Relationship to Other Skills": drop the two stale rows (`design-brainstorming`, `reverse-write-spec` — pre-rename relics) and replace the section body with:

```markdown
- **`cochallenge`**: evaluates the *decision artifacts* (direction/spec/plan) before build; cotest evaluates the *built output* against the spec after build.
- **`cocritique`**: evaluates the shipped product against the JOB; cotest never asks whether the spec was worth building.
- **`codebug`** / **`coconsolidate`** / **`coharden`**: where cotest routes failures (logic bug / cross-screen drift / robustness gap).
```

  - Everything else (two-pass process, checklist, verification traps, self-eval gate) stays word-for-word.

- [ ] **Step 3: Edit `skills/cotest/memory-bank/TEST.md`** — heading/prose from "QA" wording to "test report" wording; keep the `columns:` frontmatter and generated marker/table untouched.

- [ ] **Step 4: Edit `skills/cotest/memory-bank/_template.md`** — apply rename mapping in prose; frontmatter fields (`title`, `date`, `artboard`, `status`) unchanged.

- [ ] **Step 5: Verify**

Run: `node skills/cotest/index.mjs && node scripts/validate-skills.js`
Expected: `updated cotest/TEST.md (0 entries)`; validation passes.

- [ ] **Step 6: Commit**

```bash
git add -A skills/cotest
git commit -m "feat: rename coverify to cotest (QA testing against spec)"
```

---

### Task 3: Create the `cochallenge` skill

**Files:**
- Create: `skills/cochallenge/SKILL.md`
- Create: `skills/cochallenge/memory-bank/CHALLENGE.md`
- Create: `skills/cochallenge/memory-bank/_template.md`
- Create: `skills/cochallenge/index.mjs` (byte-identical copy of `skills/codirect/index.mjs`)

**Interfaces:**
- Produces: skill `cochallenge`, doer `cochallenger`, index `CHALLENGE.md`, verdicts `HOLDS | HOLED | COLLAPSES | UNKNOWN` — used by Tasks 4–7.

- [ ] **Step 1: Create `skills/cochallenge/SKILL.md`** with exactly:

```markdown
---
name: cochallenge
description: The "challenge" loop — the devil's advocate evaluator for decision artifacts BEFORE anything is built. Its object is the direction pitch (codirect), the spec (cospecify), or the plan (coplan) — never the built output (that's cotest) and never the shipped product (that's cocritique). Attacks the artifact from outside its own frame — first principles, problem-solution fit, blindspot hunting, assumption stress-tests, "what would make this wrong", cheapest falsification — and returns ONE verdict: HOLDS (proceed) / HOLED (specific holes, back to the generator) / COLLAPSES (a load-bearing premise fails, re-direct) / UNKNOWN (hinges on missing evidence, route to coresearch). Enforces the generator/evaluator split: the challenger is never the agent that generated the artifact, runs with fresh context, reads only the artifact + raw inputs. Findings are a signal, never truth; it never edits the artifact. Use after codirect or cospecify in upstream-heavy work, or on demand when a decision feels unexamined. The doer is cochallenger. Best on Opus (judgment-heavy).
---

# cochallenge — challenge the decision before it's built

The doer is **cochallenger** (Opus — judgment-heavy). This is the **evaluator half** of the
generator/evaluator split for **decision artifacts**: the direction pitch, the spec, the plan.
A generator loop must not be the judge of its own output — this loop is that judge, and it is
adversarial by design. Upstream mistakes are the most expensive; a wrong pitch executed
flawlessly wastes the entire chain downstream.

**The evaluator triad — three objects, never confused:**

| Evaluator | Object | Question |
|---|---|---|
| **`cochallenge`** | decision artifact, **pre-build** | Does the reasoning survive attack? |
| `cotest` | built output | Does it match the spec? |
| `cocritique` | shipped product | Does it do the job? |

`coresearch` stays the evidence loop: it tests assumptions with *new data*. cochallenge attacks
reasoning with *what already exists* — when an attack needs new data, it routes the question to
`coresearch` rather than researching itself.

## Separation rules (the whole point)

1. **The challenger is never the generator.** Never spawn the agent that produced the artifact
   (or its doer type) to challenge it.
2. **Fresh context.** The challenger reads only the artifact under challenge + `raw/` inputs +
   `STATE.md`. It does not read the generator's working transcript or rationale beyond what the
   artifact itself states — a defense that only lives in the author's head is a hole.
3. **Signal, never truth.** Findings never rewrite the artifact and never become the SSOT
   (`docs/cocreator/SSOT.md`). The generator loop re-runs with the hole list; the human arbitrates
   an unbreakable conflict.

## The attack set

Run every angle; report only what draws blood. For each finding: the claim attacked, the attack,
what would settle it.

1. **First principles** — strip the artifact to its load-bearing premises. Does each hold on its
   own, or only by momentum ("we already decided")?
2. **Problem-solution fit** — does the proposed solution actually reach the named problem? Would
   the target user recognize this as solving their problem?
3. **Blindspots** — what is conspicuously absent: unnamed risks, ignored segments, missing failure
   modes, the alternative nobody wrote down?
4. **Assumption stress-test** — take each named assumption and push: what breaks it cheapest?
   Which unnamed assumption is doing the most work?
5. **Self-deception check** — where is the artifact grading its own homework: appetite backed into
   from a deadline, evidence cherry-picked, a "validated" claim that was never tested?
6. **Falsification** — what is the cheapest observation that would prove this direction/spec/plan
   wrong? If none exists, that is itself a finding.

## The verdict (exactly one)

| Verdict | Condition | Route |
|---|---|---|
| **HOLDS** | Artifact survives the attack set; remaining findings are minor | Proceed to the next loop in the chain |
| **HOLED** | Specific, fixable holes found | Back to the generator loop (`codirect` / `cospecify` / `coplan`) with the hole list |
| **COLLAPSES** | A load-bearing premise fails | → `codirect` (re-direct) + `inbox/` decision ask — never banked unattended |
| **UNKNOWN** | The challenge hinges on evidence that doesn't exist yet | → `coresearch` with the cheapest test named |

Every verdict carries its load-bearing findings and — for HOLDS — what was attacked and survived
(a HOLDS with no recorded attacks is a rubber stamp, not a verdict).

## Memory Bank

Save each challenge as `YYYY-MM-DD-kebab-title.md`:
```
.agents/skills/cochallenge/memory-bank/2026-08-11-onboarding-pitch.md
```
Copy `_template.md`, keep its frontmatter (`title`, `date`, `object`, `artifact`, `verdict`,
`status`) filled — `object` is `direction | spec | plan`, `artifact` is the path of what was
challenged. `CHALLENGE.md` is the auto-generated index — never edit it by hand. After changes:
`node .agents/skills/cochallenge/index.mjs`.

## Self-eval gate (close the loop)

- **Attack set run, one verdict issued with findings, route named** → PASS. Route per the
  verdict table.
- **Findings are all taste-level nitpicks** → report HOLDS and say the artifact didn't warrant
  the challenge; don't manufacture holes to justify the loop.
- **The challenged artifact can't be located or is still a draft-in-motion** → BLOCKED; challenge
  a moving target and both loops thrash.
- **Backprop:** a HOLDS artifact that later COLLAPSES in the market, or a HOLED verdict the
  generator was right to reject, hands a lesson to `colearn`.

## Guardrails

- **Attack the artifact, not the author.** Findings name a claim and an attack, never a vibe.
- **No research.** Needing new data is a routing decision (`coresearch`), not a license to browse.
- **No edits.** The challenger that "fixes" the pitch has become a second generator — the split
  is dead and the loop has failed.
- **Bounded.** One challenge round per artifact version; re-challenge only after the generator
  ships a new version. Endless adversarial ping-pong is cancel-not-extend territory.
- **Don't duplicate `cocritique`.** If the thing under scrutiny has shipped to users, hand off —
  the five-lens ladder and the evidence rule live there.
```

- [ ] **Step 2: Create `skills/cochallenge/memory-bank/CHALLENGE.md`** with exactly:

```markdown
---
columns: Date=date, Challenge=title*, Object=object, Verdict=verdict, Status=status
---

# Challenge index

Auto-generated index of cochallenge records — one row per challenged decision artifact.
Do not edit the table by hand; run `node index.mjs` in the skill directory.

<!-- The table below is auto-generated by the skill indexer -->
_No records yet. Add `.md` files to this folder to populate the index._
```

- [ ] **Step 3: Create `skills/cochallenge/memory-bank/_template.md`** with exactly:

```markdown
---
title: What was challenged
date: YYYY-MM-DD
object: direction | spec | plan
artifact: path/to/challenged/artifact.md
verdict: HOLDS | HOLED | COLLAPSES | UNKNOWN
status: open
---

# Challenge — {artifact title}

## Object

What artifact, which version (commit/date), produced by which loop.

## Attacks run

| # | Angle | Claim attacked | Attack | Outcome (survived / drew blood) |
|---|---|---|---|---|

## Findings (the holes)

### 1. {Finding title}
- **Claim**: what the artifact asserts
- **Attack**: why it may not hold
- **Settles it**: the cheapest observation/decision that would resolve this

## Verdict

`HOLDS | HOLED | COLLAPSES | UNKNOWN` — plus the route taken (generator re-run / codirect /
coresearch) and, for HOLDS, what was attacked and survived.
```

- [ ] **Step 4: Copy the indexer**

```bash
cp skills/codirect/index.mjs skills/cochallenge/index.mjs
```

- [ ] **Step 5: Verify**

Run: `node skills/cochallenge/index.mjs && node scripts/validate-skills.js`
Expected: `updated cochallenge/CHALLENGE.md (0 entries)`; validation passes (name==dir, description ≤1024, file ≤500 lines).

- [ ] **Step 6: Commit**

```bash
git add skills/cochallenge
git commit -m "feat: add cochallenge - devil's advocate evaluator for decision artifacts"
```

---

### Task 4: Regenerate agents + manifests from the updated ROSTER

**Files:**
- Modify: `scripts/build-plugin-manifests.js` (ROSTER, PURPOSES, EXTRA_STEPS)
- Delete: `agents/coframer.md`, `agents/coverifier.md`, `.opencode/agents/coframer.md`, `.opencode/agents/coverifier.md` (generator writes new files but never deletes stale ones)
- Generated (by running the script): `agents/codirector.md`, `agents/cotester.md`, `agents/cochallenger.md`, `.opencode/agents/` equivalents, all plugin/marketplace JSONs

**Interfaces:**
- Consumes: skill names/doers from Tasks 1–3.
- Produces: `cocreation:codirector`, `cocreation:cotester`, `cocreation:cochallenger` agent types referenced by cocreator (Task 5).

- [ ] **Step 1: Edit ROSTER in `scripts/build-plugin-manifests.js`**

Replace the three affected lines / insert one (keep the stage order — cochallenge sits right after cospecify, the last generator before build):

```js
const ROSTER = [
  ['codirect', 'codirector', 'opus'],
  ['coresearch', 'coresearcher', 'opus'],
  ['costudy', 'costudier', 'sonnet'],
  ['coplan', 'coplanner', 'sonnet'],
  ['cospecify', 'cospecifier', 'sonnet'],
  ['cochallenge', 'cochallenger', 'opus'],
  ['cobuild', 'cobuilder', 'sonnet'],
  ['cotest', 'cotester', 'haiku'],
  ['codebug', 'codebugger', 'opus'],
  ['cochangelog', 'cochangelogger', 'haiku'],
  ['colearn', 'colearner', 'sonnet'],
  ['cocritique', 'cocritic', 'opus'],
  ['coconsolidate', 'coconsolidator', 'sonnet'],
  ['coharden', 'cohardener', 'sonnet'],
  ['codraw', 'codrawer', 'sonnet'],
  ['cotranslate', 'cotranslator', 'sonnet'],
]
```

- [ ] **Step 2: Edit PURPOSES** — rename keys and update the two clauses; add cochallenge:

```js
  codirect:
    'set product + design direction, fix the appetite, and write a tight pitch',
  cotest: 'systematic QA testing of built output against design artboards or specs',
  cochallenge:
    'devil-advocate the direction/spec/plan pre-build — stress-test reasoning, surface blindspots',
```

(Delete the old `coframe:` and `coverify:` entries.)

- [ ] **Step 3: Add EXTRA_STEPS for cochallenge** (keep the existing cocritique entry):

```js
  cochallenge: [
    'You are the evaluator, never the generator: challenge only artifacts you did not produce, from fresh context — read the artifact + raw inputs, not its author’s reasoning.',
    'Issue exactly one verdict — HOLDS / HOLED / COLLAPSES / UNKNOWN — with the findings that carry it. Never edit the artifact; route instead (generator re-run, codirect, or coresearch).',
  ],
```

- [ ] **Step 4: Remove stale generated agents and regenerate**

```bash
git rm agents/coframer.md agents/coverifier.md .opencode/agents/coframer.md .opencode/agents/coverifier.md
npm run build-manifests
```

Expected output: `Generated 6 manifests for 17 skills and 16 agents for Claude/Codex/Cursor/OpenCode.`

- [ ] **Step 5: Verify no stale generated refs**

Run: `grep -rl 'coframer\|coverifier' agents/ .opencode/agents/ .claude-plugin/ || echo CLEAN`
Expected: `CLEAN`

- [ ] **Step 6: Commit**

```bash
git add -A scripts/build-plugin-manifests.js agents .opencode .claude-plugin .codex-plugin .cursor-plugin .agents/plugins
git commit -m "feat: roster codirect/cotest/cochallenge; cotest on haiku; regen manifests"
```

---

### Task 5: Rewire cocreator (SKILL.md + workflows.md + templates)

**Files:**
- Modify: `skills/cocreator/SKILL.md`
- Modify: `skills/cocreator/references/workflows.md`
- Modify: `skills/cocreator/references/STATE.template.md` (rename sweep only)

**Interfaces:**
- Consumes: names/verdicts from Tasks 1–4.

- [ ] **Step 1: `skills/cocreator/SKILL.md` roster table** — apply the rename mapping to the `coframe` and `coverify` rows (cotest row model becomes **haiku**, "When" becomes `test behavior + visuals vs spec — cross-check only *(mandatory, core)*`), and insert after the `cospecify` row:

```markdown
| `cochallenge` | cochallenger | challenge (pre-build) | **opus** | devil-advocate the direction/spec/plan before build — blindspots, first principles, problem-solution fit |
```

Update the model-tiers line to note haiku covers mechanical capture/format *and checklist cross-checking*.

- [ ] **Step 2: `skills/cocreator/SKILL.md` §1 diagram + workflow table** — replace the ASCII diagram line with:

```
codirect → [cochallenge] → coresearch → coplan → cospecify → [cochallenge] → ╔ cobuild ⇄ cotest (↘codebug) ╗ → cochangelog
           + costudy                                                          ╚═══════ colearn (learn) ═════╝
```

Workflow-table rows change to:
- **discover**: `colearn(recall) → codirect → cochallenge → coresearch`, optionally alongside `costudy`
- **feature**: `colearn → codirect → cochallenge → coresearch → coplan → cospecify → cochallenge → [codraw] → cobuild ⇄ cotest → coharden → cochangelog → colearn`
- **design-first**: `[costudy] → cospecify → cochallenge → codraw → cotranslate → cotest → cochangelog`
- every other `coverify` → `cotest`, `coframe` → `codirect` in place.

The `evaluate` router table gains a fourth row:

```markdown
| the **reasoning** | does the decision survive attack? | `cochallenge` | → codirect (COLLAPSES) · generator re-run (HOLED) · discover/coresearch (UNKNOWN) |
```

- [ ] **Step 3: `skills/cocreator/SKILL.md` remaining prose** — rename sweep (§3 examples `cocreation:coframer`→`cocreation:codirector`, "coverify is never skipped"→"cotest is never skipped", SSOT ladder `intent (coframe)`→`intent (codirect)`, diagnostic-loop list gains `cochallenge`, §4 routing text, principles). Add to §4 decision tree, after the DRIFT bullet:

```markdown
- **HOLED / COLLAPSES** (from `cochallenge`) → the generator loop re-runs with the hole list
  (HOLED), or the chain returns to `codirect` (COLLAPSES — a direction change, stop condition 4).
```

- [ ] **Step 4: `skills/cocreator/references/workflows.md`** — same rewiring:
  - "Choosing one" question-2 table gains the `the reasoning` row (same as SKILL.md above).
  - `discover` chain becomes: `colearn(recall) → codirect → cochallenge → coresearch → …` with one added line under it: `cochallenge stress-tests the pitch before evidence is spent on it: HOLED → codirect re-runs; COLLAPSES → stop (not building is the result); UNKNOWN folds into coresearch's queue.`
  - `feature` chain becomes: `colearn(recall) → codirect → cochallenge → coresearch → coplan → cospecify → cochallenge → [codraw] → cobuild ⇄ cotest (↘ codebug) → coharden → cochangelog → colearn(capture)` — and its **Skips** line gains: `drop the second cochallenge when the spec is a thin restatement of an already-HOLDS pitch.`
  - `design-first` chain becomes: `[costudy] → cospecify → cochallenge → codraw → cotranslate → cotest → cochangelog` — Skips line gains: `drop cochallenge when the spec merely transcribes an existing approved design.`
  - `ship`, `fix`, `evaluate`, `release-prep`, `cleanup`: rename sweep only (`coverify`→`cotest`, `coframe`→`codirect`) — **no cochallenge inserted** (spec §5: small chains don't pay the challenge overhead).
  - `evaluate` router block gains the `vs the reasoning → cochallenge` line; Chaining section and Rules 1/5 get the rename sweep; Rule 5's diagnostic list gains `cochallenge`.

- [ ] **Step 5: `skills/cocreator/references/STATE.template.md`** — rename sweep only.

- [ ] **Step 6: Verify + commit**

Run: `grep -rn 'coframe\|coverify' skills/cocreator/ || echo CLEAN`
Expected: `CLEAN`

```bash
git add skills/cocreator
git commit -m "feat: wire cochallenge into discover/feature/design-first; rename sweep in cocreator"
```

---

### Task 6: Update docs, README, AGENTS.md, CHANGELOG, package.json description

**Files:**
- Modify: `docs/cocreator/ROADMAP.md`, `docs/cocreator/SSOT.md`, `docs/cocreator/PLAYBOOK.md`, `docs/cocreator/RESEARCH.md` (whichever of the latter two contain matches)
- Modify: `README.md`, `AGENTS.md`, `CHANGELOG.md`, `package.json` (description only)
- Leave: `docs/cocreator/specs/2026-08-05-costudy.md` untouched (historical)

- [ ] **Step 1: `docs/cocreator/ROADMAP.md`**
  - Architecture diagram: `coframe`→`codirect`, `coverify`→`cotest`, insert `[cochallenge]` after codirect and after cospecify (same shape as cocreator §1 diagram in Task 5).
  - Roster table: rename the two rows (cotest model → **Haiku**), add row after `cospecify`:

```markdown
| `cochallenge` | `cochallenger` | NEW | Challenge: devil-advocate the direction/spec/plan pre-build — generator/evaluator split | Opus | Optional |
```

  - Count line → `17 skills (1 master + 16 loops).`
  - Extend the **Renamed / merged** section with a new paragraph:

```markdown
**Renamed / added (v0.15.0).** `coframe` → **`codirect`** — the Director-of-Product seat (Shape Up
shaping): product direction + design direction at direction level, index `DIRECTION.md`.
`coverify` → **`cotest`** — QA testing strictly against the spec, index `TEST.md`, moved to Haiku
(checklist cross-checking is mechanical). New **`cochallenge`** (`cochallenger`, Opus,
`CHALLENGE.md`) — the missing evaluator for decision artifacts pre-build; enforces the
generator/evaluator split (the challenger is never the generator; findings are signal, never
truth). Evaluator triad: cochallenge (vs reasoning, pre-build) · cotest (vs spec, post-build) ·
cocritique (vs the job, post-ship). The installer's `RENAMES` covers both renames.
```

  - Rename sweep for every other mention (two-feedback-edges paragraph, workflow-layer bullets, SSOT paragraph, native-tooling `/verify (coverify)` → `(cotest)`, build-sequence names — build-sequence letters are historical; sweep names only where they'd mislead, leave the D-list as history with `(now codirect)` parenthetical).
- [ ] **Step 2: `docs/cocreator/SSOT.md` + `PLAYBOOK.md` + `RESEARCH.md`** — mechanical rename sweep (`coframe`→`codirect`, `coframer`→`codirector`, `coverify`→`cotest`, `coverifier`→`cotester`); in SSOT.md's diagnostic-loop lists, add `cochallenge` alongside the others.
- [ ] **Step 3: `README.md` + `AGENTS.md`** — rename sweep; roster/tables gain the cochallenge row (copy wording from ROADMAP row above); any loop-count mention → 16 loops / 17 skills.
- [ ] **Step 4: `CHANGELOG.md`** — prepend:

```markdown
## 0.15.0 — 2026-08-11

- `coframe` → `codirect` (Director of Product: product + design direction, appetite, pitch; index `DIRECTION.md`)
- `coverify` → `cotest` (QA testing strictly vs spec; index `TEST.md`; model → Haiku)
- New `cochallenge` — devil's advocate evaluator for decision artifacts pre-build (`cochallenger`, Opus, index `CHALLENGE.md`); wired into discover/feature/design-first after codirect and cospecify; evaluate router gains "vs the reasoning"
- Generator/evaluator split enforced: challenger is never the generator; findings are signal, never truth
```

- [ ] **Step 5: `package.json` description** → `An ecosystem of co-* loop skills for human-AI product co-creation — direct, challenge, research, plan, specify, draw, build, test, debug, document, and learn — orchestrated by /cocreator.`
- [ ] **Step 6: Commit**

```bash
git add docs README.md AGENTS.md CHANGELOG.md package.json
git commit -m "docs: roster/workflow docs for codirect, cotest, cochallenge"
```

---

### Task 7: Installer RENAMES + sweep remaining skill cross-refs

**Files:**
- Modify: `cli.js` (RENAMES map + any other matches)
- Modify: every remaining file with live refs — per the Task-0 grep these are: `skills/cobuild/SKILL.md`, `skills/cobuild/memory-bank/_template.md`, `skills/coconsolidate/SKILL.md`, `skills/cocritique/SKILL.md` + `memory-bank/_template.md` + `references/{checklists,gotchas,report-template}.md`, `skills/codebug/SKILL.md`, `skills/codraw/SKILL.md` + `memory-bank/_template.md` + `references/{design-manifest,gotchas,naming-and-ledger}.md`, `skills/coharden/SKILL.md` + `memory-bank/_template.md`, `skills/colearn/SKILL.md`, `skills/coplan/SKILL.md`, `skills/coresearch/SKILL.md`, `skills/cospecify/SKILL.md` + `memory-bank/_template.md`, `skills/costudy/SKILL.md`, `skills/cotranslate/SKILL.md` + `references/gotchas.md`

- [ ] **Step 1: `cli.js` RENAMES** — old names must point at the *current* name (chase the chain: `coshape` pointed at `coframe`, which is now `codirect`):

```js
const RENAMES = {
  coshape: 'codirect',
  coframe: 'codirect',
  codesign: 'cospecify',
  coport: 'cotranslate',
  coaudit: 'coconsolidate',
  coverify: 'cotest',
}
```

Also rename-sweep any other `coframe`/`coverify` mention in `cli.js` prose/help text.

- [ ] **Step 2: Sweep the listed skill files** — apply the rename mapping (prose-aware, not sed). Special cases:
  - `skills/cocritique/SKILL.md`: the boundary table ("The line vs siblings") gains a `cochallenge` row: `| \`cochallenge\` | decision artifact **pre-build** | Did the reasoning survive attack before we built it? |` — and the frontmatter description's "NOT coverify (impl vs spec)" → "NOT cotest (impl vs spec)".
  - `skills/coresearch/SKILL.md`: where it names coframe as its upstream, note it also receives UNKNOWN routes from `cochallenge`.
  - Everything else: pure rename.

- [ ] **Step 3: Verify zero live refs**

Run: `grep -rn 'coframe\|coframer\|coverify\|coverifier' --include='*.md' --include='*.js' --include='*.mjs' --include='*.json' . | grep -v node_modules | grep -v '.git/' | grep -v CHANGELOG.md | grep -v 'docs/cocreator/specs/' | grep -v 'docs/superpowers/' || echo CLEAN`
Expected: `CLEAN` (superpowers spec/plan mention old names by design — they document the rename).

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: installer RENAMES + cross-ref sweep for codirect/cotest"
```

---

### Task 8: Version bump, full validation, PR

**Files:**
- Modify: `package.json` (version → `0.15.0`)
- Regenerate: manifests (version is embedded in the JSONs)

- [ ] **Step 1: Bump version** — `package.json` `"version": "0.15.0"`.
- [ ] **Step 2: Full pipeline**

Run: `npm run update-indices && npm run build-manifests && npm run validate`
Expected: all indices regenerate (now including `codirect/DIRECTION.md`, `cotest/TEST.md`, `cochallenge/CHALLENGE.md`), manifests report **17 skills / 16 agents**, validation passes.

- [ ] **Step 3: Re-run the Task 7 grep** — still `CLEAN`.
- [ ] **Step 4: Commit + PR**

```bash
git add -A
git commit -m "chore: bump to 0.15.0"
git push -u origin feat/generator-evaluator-split
gh pr create --title "feat: generator/evaluator split — codirect, cotest, cochallenge (0.15.0)" --body "..."
```

PR body: summary of the spec's Decisions section + link to `docs/superpowers/specs/2026-08-11-generator-evaluator-split-design.md`. End with the standard generated-with footer.

---

## Self-review notes

- Spec coverage: §1 codirect → Task 1; §2 cotest (incl. Haiku) → Tasks 2+4; §3 cochallenge → Tasks 3+4; §4 triad → cocritique/cotest boundary text (Tasks 2, 7) ; §5 wiring → Task 5; §6 migration → Tasks 4, 6, 7, 8.
- `index.mjs` is generic (ALL-CAPS index discovery) — DIRECTION/TEST/CHALLENGE all match `^[A-Z]+\.md$`; no indexer edits needed.
- The pre-commit hook auto-runs update-indices; harmless here.
- CI publishes on merge to main when the version is new — 0.15.0 releases on PR merge, which is intended.
